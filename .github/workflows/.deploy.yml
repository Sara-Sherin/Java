name: Java WAR Deploy to Tomcat

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout source
        uses: actions/checkout@v4

      - name: Setup Java 21
        uses: actions/setup-java@v4
        with:
          distribution: corretto
          java-version: '21'
          cache: maven

      - name: Build WAR
        run: mvn clean package

      - name: Verify WAR generated
        run: ls -lah target/

      - name: Copy WAR to EC2
        uses: appleboy/scp-action@v0.1.7
        with:
          host: ${{ secrets.EC2_HOST }}
          username: ${{ secrets.EC2_USER }}
          key: ${{ secrets.EC2_SSH_KEY }}
          port: 22
          source: "target/*.war"
          target: "/home/ec2-user/deploy-temp"

      - name: Deploy to Tomcat
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: ${{ secrets.EC2_HOST }}
          username: ${{ secrets.EC2_USER }}
          key: ${{ secrets.EC2_KEY }}
          port: 22
          script: |
            sudo mkdir -p /opt/tomcat/webapps

            sudo rm -f /opt/tomcat/webapps/ROOT.war

            sudo mv /home/ec2-user/deploy-temp/target/*.war /opt/tomcat/webapps/ROOT.war

            sudo systemctl restart tomcat

            sudo systemctl status tomcat --no-pager
